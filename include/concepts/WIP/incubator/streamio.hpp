Stream I / O functions UnformattedInputFunction

               a stream input function that does not skip leading whitespace and
    counts the processed characters(concept) FormattedInputFunction

        a stream input function that skips leading whitespace(concept)
            UnformattedOutputFunction

                a basic stream output function(concept) FormattedOutputFunction

                    a stream output function that sets failbit on errors and
    returns a reference to the stream(concept)