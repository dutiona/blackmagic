
Container Container

        data structure that allows element access using iterators(concept)
            ReversibleContainer

    container using bidirectional iterators(concept) AllocatorAwareContainer

    (C++ 11)

        container using an allocator(concept) SequenceContainer

    container with elements stored linearly(concept) ContiguousContainer

    (C++ 17)

        container with elements stored at adjacent memory
    addresses(concept) AssociativeContainer

    container that stores elements by associating them to keys(concept)
        UnorderedAssociativeContainer

    (C++ 11)

        container that stores elements stored in buckets by associating them to
    keys(concept) Container element DefaultInsertable

    (C++ 11)

        element can be default
    - constructed in uninitialized storage(concept) CopyInsertable

    (C++ 11)

        element can be copy
    - constructed in uninitialized storage(concept) MoveInsertable

    (C++ 11)

        element can be move
    - constructed in uninitialized storage(concept) EmplaceConstructible

    (C++ 11)

        element can be constructed in uninitialized storage(concept) Erasable

    (C++ 11)

        element can be destroyed using an allocator(concept)