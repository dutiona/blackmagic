Random Number Generation SeedSequence

    (C++ 11)

        consumes a sequence of integers and produces a sequence of 32 -
    bit unsigned values(concept) UniformRandomBitGenerator

    (C++ 11)

        returns uniformly distributed random unsigned integers(concept)
            RandomNumberEngine

    (C++ 11)

        a deterministic UniformRandomBitGenerator,
    defined by the seed(concept) RandomNumberEngineAdaptor

    (C++ 11)

        a RandomNumberEngine that transforms the output of another
    RandomNumberEngine(concept) RandomNumberDistribution

    (C++ 11)

        returns random numbers distributed according to a given mathematical
    probability density function(concept)